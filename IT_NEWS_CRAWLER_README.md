# IT新闻爬虫使用说明

## 功能概述

这个IT新闻爬虫可以自动爬取每日IT行业最新新闻，并通过智能算法筛选出热度最高的10条新闻。

## 文件说明

- `simple_it_news_crawler.py` - 简化版爬虫（推荐使用）
- `it_news_crawler.py` - 完整版爬虫（功能更全面）
- `requirements.txt` - 依赖包列表

## 安装依赖

```bash
pip3 install -r requirements.txt
```

## 使用方法

### 基本使用

```bash
python3 simple_it_news_crawler.py
```

### 功能特点

1. **多源爬取**：支持多个IT新闻源
   - 36氪（中文）
   - TechCrunch（英文）
   - Hacker News（英文）
   - 模拟数据（用于演示）

2. **智能热度评分**：
   - 关键词权重匹配
   - 时间新鲜度评分
   - 标题长度优化
   - 来源权威性权重

3. **多种输出格式**：
   - 控制台显示
   - JSON文件保存
   - Excel文件导出

## 热度评分算法

### 评分因素

1. **关键词权重**（权重最高）
   - AI相关：10分
   - 人工智能：10分
   - 机器学习：8分
   - 区块链：7分
   - 5G：8分
   - 量子计算：8分
   - 等等...

2. **时间新鲜度**
   - 今天发布：2.0倍
   - 昨天发布：1.5倍
   - 小时前：1.8倍
   - 分钟前：2.0倍

3. **标题长度优化**
   - 10-50字符：1.2倍
   - 其他长度：1.0倍或0.8倍

4. **来源权威性**
   - 36氪：1.2倍
   - TechCrunch：1.1倍
   - Hacker News：1.0倍

### 最终分数计算

```
最终分数 = 基础分数 × (1 + 关键词分数 × 0.1) × 时间分数 × 长度分数 × 来源分数
```

## 输出示例

```
🔥 今日IT新闻热度排行榜 (Top 10)

====================================================================================================

1. 【模拟数据】苹果发布M3芯片，AI性能提升60%
   🔥 热度分数: 9.6
   ⏰ 发布时间: 今天 09:15
   📝 摘要: 苹果在最新发布会上推出M3芯片，专为AI和机器学习任务优化
   🔗 链接: https://example.com/apple-m3
----------------------------------------------------------------------------------------------------

2. 【模拟数据】OpenAI发布GPT-4 Turbo，性能提升显著
   🔥 热度分数: 8.64
   ⏰ 发布时间: 今天 10:30
   📝 摘要: OpenAI宣布推出GPT-4 Turbo版本，在保持高质量输出的同时大幅提升了处理速度
   🔗 链接: https://example.com/gpt4-turbo
----------------------------------------------------------------------------------------------------
```

## 生成文件

运行后会生成以下文件：

1. `it_news_top10_YYYYMMDD_HHMMSS.json` - JSON格式的新闻数据
2. `it_news_top10_YYYYMMDD_HHMMSS.xlsx` - Excel格式的新闻数据
3. `it_news_crawler.log` - 运行日志

## 自定义配置

### 修改关键词权重

在 `simple_it_news_crawler.py` 中修改 `keyword_weights` 字典：

```python
self.keyword_weights = {
    'AI': 10,
    '人工智能': 10,
    '你的关键词': 权重值,
    # ...
}
```

### 添加新闻源

在 `news_sources` 列表中添加新的新闻源：

```python
{
    'name': '新闻源名称',
    'url': '新闻源URL',
    'type': 'html',
    'selectors': {
        'title': '标题选择器',
        'link': '链接选择器',
        'time': '时间选择器',
        'summary': '摘要选择器'
    }
}
```

### 调整输出数量

修改 `get_top_news()` 方法的 `limit` 参数：

```python
top_news = crawler.get_top_news(20)  # 获取前20条
```

## 注意事项

1. **网络连接**：需要稳定的网络连接访问新闻网站
2. **反爬虫机制**：某些网站可能有反爬虫措施，建议适当调整请求间隔
3. **数据准确性**：模拟数据仅用于演示，实际使用时需要确保新闻源的可访问性
4. **法律合规**：请遵守相关网站的使用条款和robots.txt规则

## 故障排除

### 常见问题

1. **依赖包安装失败**
   ```bash
   pip3 install --upgrade pip
   pip3 install -r requirements.txt
   ```

2. **网络连接超时**
   - 检查网络连接
   - 增加timeout参数值

3. **Excel导出失败**
   - 确保pandas和openpyxl已正确安装
   - 检查文件写入权限

4. **新闻源无法访问**
   - 检查网站是否可访问
   - 更新User-Agent和请求头
   - 检查网站结构是否发生变化

## 扩展功能

### 定时运行

可以使用cron定时任务每天自动运行：

```bash
# 编辑crontab
crontab -e

# 添加定时任务（每天上午9点运行）
0 9 * * * cd /path/to/project && python3 simple_it_news_crawler.py
```

### 邮件通知

可以添加邮件发送功能，将结果发送到指定邮箱。

### 数据库存储

可以将新闻数据存储到数据库中，便于历史数据查询和分析。

## 许可证

本项目采用MIT许可证，详见LICENSE文件。