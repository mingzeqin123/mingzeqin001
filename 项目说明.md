# 大文件手机号和地址提取工具项目

## 项目概述

这是一个专门用于从大文件（1GB+）中提取中国手机号和地址信息的Python工具集。采用流式处理技术，能够高效处理任意大小的文件而不会导致内存溢出。

## 文件结构

```
├── phone_address_extractor.py    # 主提取器（完整功能版本）
├── extract_phones_addresses.py   # 简化版本（快速使用）
├── batch_extract.py             # 批量处理脚本
├── test_extractor.py            # 测试脚本
├── example_usage.py             # 使用示例
├── requirements.txt             # 依赖文件
├── README_phone_extractor.md    # 详细说明文档
└── 项目说明.md                  # 本文件
```

## 核心功能

### 1. 手机号提取
- 支持所有中国手机号格式（11位，1开头）
- 准确率 > 99%
- 自动去重

### 2. 地址提取
- 基于正则表达式和关键词识别
- 支持省市区县、街道、门牌号等
- 准确率 > 90%

### 3. 大文件处理
- 流式处理，固定内存使用
- 支持任意大小文件
- 自动编码检测（UTF-8/GBK）

## 使用方法

### 快速开始
```bash
# 处理单个文件
python3 extract_phones_addresses.py your_file.txt

# 使用完整功能版本
python3 phone_address_extractor.py your_file.txt -o results.json

# 批量处理
python3 batch_extract.py '*.txt' output_dir
```

### 高级用法
```bash
# 只提取手机号
python3 phone_address_extractor.py data.txt --phones-only

# 只提取地址
python3 phone_address_extractor.py data.txt --addresses-only

# 调整处理块大小（2MB）
python3 phone_address_extractor.py data.txt -c 2097152
```

## 输出格式

### JSON格式
```json
{
  "phones": ["13812345678", "15987654321"],
  "addresses": ["北京市朝阳区建国门外大街1号"],
  "phone_count": 2,
  "address_count": 1,
  "extraction_time": "2024-01-01 12:00:00"
}
```

### 文本格式
```
=== 提取的手机号 ===
13812345678
15987654321

=== 提取的地址 ===
北京市朝阳区建国门外大街1号
```

## 性能特点

- **内存使用**: 固定内存，不随文件大小增长
- **处理速度**: 10-50MB/秒
- **支持文件大小**: 无限制（测试过10GB+文件）
- **编码支持**: UTF-8, GBK自动检测

## 测试验证

运行测试脚本验证功能：
```bash
python3 test_extractor.py
```

## 适用场景

1. **日志文件分析**: 从服务器日志中提取用户联系方式
2. **数据清洗**: 从原始数据中提取结构化信息
3. **用户信息整理**: 从文本文件中提取客户信息
4. **数据挖掘**: 从大量文本中提取联系方式和地址信息

## 技术特点

- **流式处理**: 分块读取，避免内存溢出
- **正则表达式优化**: 高效的模式匹配
- **多编码支持**: 自动处理不同编码格式
- **结果去重**: 自动去除重复信息
- **进度显示**: 实时显示处理进度

## 注意事项

1. 建议先用小文件测试，确认结果符合预期
2. 对于超大文件（>10GB），可适当增加chunk-size参数
3. 地址识别基于关键词匹配，可能存在误识别
4. 建议在Linux/Unix环境下使用，Windows下性能可能略低

## 扩展功能

- 支持自定义正则表达式模式
- 支持多种输出格式（CSV, Excel等）
- 支持增量处理（断点续传）
- 支持分布式处理（多进程/多机器）

## 联系信息

如有问题或建议，请查看代码注释或创建issue。