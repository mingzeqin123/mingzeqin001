pandas>=1.3.0
openpyxl>=3.0.0

# Python邮件发送依赖 / Email sending dependencies
# 注意：Python内置了发送邮件所需的所有库，无需额外安装
# Note: Python includes all required libraries for email sending:
# - smtplib: SMTP客户端 / SMTP client
# - email: 邮件构建和解析 / Email construction and parsing  
# - ssl: SSL/TLS加密支持 / SSL/TLS encryption support
# - typing: 类型提示 / Type hints