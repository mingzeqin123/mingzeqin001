# 优惠券系统流程图

## 系统架构图

```
┌─────────────┐    ┌─────────────┐
│    用户     │    │   管理员    │
└─────┬───────┘    └─────┬───────┘
      │                  │
      └──────────┬───────┘
                 │
    ┌─────────────▼─────────────┐
    │      FastAPI API层        │
    │  ┌─────────────────────┐  │
    │  │   认证授权/限流控制   │  │
    │  └─────────────────────┘  │
    └─────────────┬─────────────┘
                  │
    ┌─────────────▼─────────────┐
    │        业务服务层          │
    │ ┌─────┐ ┌─────┐ ┌─────┐  │
    │ │优惠券│ │订单 │ │支付 │  │
    │ │服务 │ │服务 │ │服务 │  │
    │ └─────┘ └─────┘ └─────┘  │
    │ ┌─────┐ ┌─────┐          │
    │ │用户 │ │退款 │          │
    │ │优惠券│ │服务 │          │
    │ │服务 │ │     │          │
    │ └─────┘ └─────┘          │
    └─────────────┬─────────────┘
                  │
    ┌─────────────▼─────────────┐
    │       业务规则层           │
    │ ┌─────────────────────┐  │
    │ │ 数据验证/业务规则验证 │  │
    │ └─────────────────────┘  │
    └─────────────┬─────────────┘
                  │
    ┌─────────────▼─────────────┐
    │        数据层             │
    │ ┌─────────┐ ┌─────────┐  │
    │ │ 数据库  │ │  缓存   │  │
    │ └─────────┘ └─────────┘  │
    └───────────────────────────┘
```

## 核心业务流程

### 1. 优惠券发放流程

```
管理员 → 创建优惠券 → 设置规则 → 激活优惠券 → 用户可领取
```

### 2. 用户领取优惠券流程

```
用户 → 输入优惠券代码 → 验证优惠券 → 检查用户限制 → 创建用户优惠券记录
```

### 3. 订单创建流程

```
用户 → 选择商品 → 选择优惠券 → 计算价格 → 创建订单 → 生成支付
```

### 4. 支付流程

```
用户 → 选择支付方式 → 调用支付网关 → 支付成功 → 更新订单状态 → 标记优惠券已使用
```

### 5. 退款流程

```
用户 → 申请退款 → 验证退款条件 → 处理退款 → 恢复优惠券 → 更新订单状态
```

## 数据流转图

### 优惠券状态流转

```
创建 → 激活 → 用户领取 → 已使用 → 已退款
  ↓      ↓       ↓        ↓        ↓
待审核  可用    可用     已使用   已退款
```

### 订单状态流转

```
创建 → 待支付 → 已支付 → 已退款
  ↓       ↓       ↓       ↓
待审核   待支付   已支付   已退款
```

### 支付状态流转

```
创建 → 待支付 → 支付成功 → 已退款
  ↓       ↓        ↓        ↓
待支付   待支付    成功     已退款
```

## 系统特性

### 高可用性
- 数据库连接池
- 缓存机制
- 异常处理

### 安全性
- 数据验证
- 业务规则验证
- 权限控制

### 可扩展性
- 模块化设计
- 服务分离
- 配置化参数

### 监控性
- 日志记录
- 性能监控
- 业务指标统计